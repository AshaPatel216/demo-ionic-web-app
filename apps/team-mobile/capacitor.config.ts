import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'team-mobile',
  webDir: '../../dist/apps/team-mobile',
  bundledWebRuntime: false,
};

export default config;
